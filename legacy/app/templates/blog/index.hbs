{{#if page}}
  <GlobalHeader @page={{page}} />

  {{#each page.section as |section|}}
     <PageSection @section={{section}} @articles={{articles}} />
  {{/each}}

  <GlobalFooter />

{{else}}
  <LoadingSpinner />
{{/if}}