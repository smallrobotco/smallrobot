{{#if model}}
  <GlobalHeader @page={{model}} />

  {{#each model.section as |section|}}
    <PageSection @section={{section}} />
  {{/each}}

  <GlobalFooter />

{{else}}
  <LoadingSpinner />
{{/if}}